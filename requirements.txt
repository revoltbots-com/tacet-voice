# Core dependencies
numpy>=1.24.0
sounddevice>=0.4.6
soundfile>=0.12.1
faster-whisper>=0.10.0
pynput>=1.7.6
requests>=2.31.0

# GUI
customtkinter>=5.2.0

# Clipboard integration
pyperclip>=1.8.0

# System tray
pystray>=0.19.0
Pillow>=9.0.0

# Windows-specific (for auto-start feature)
pywin32>=305; sys_platform == 'win32'
winshell>=0.6; sys_platform == 'win32'

# Optional: For GPU support (CUDA)
# Uncomment if you have NVIDIA GPU and want faster transcription
# ctranslate2>=3.20.0  # GPU-enabled version
